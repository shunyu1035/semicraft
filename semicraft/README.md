semicraft
========

安装
----

- 通过 PyPI 安装（推荐）：

	pip install semicraft

- 系统依赖（用于本地构建或编译扩展时）：

	- Eigen（C++ 线性代数库，头文件安装即可，通常包名为 `eigen3`）
	- pybind11（用于 Python/C++ 绑定）
	- CMake 与 C++ 构建工具（Windows 下建议安装 Visual Studio Build Tools）

